{
  "rules": {
    ".read": "auth.provider === 'google'",
    ".write": "auth.provider === 'google'",
  	"misjaquecas": {
  		"$uid": {
        ".read": "$uid === auth.uid",
        ".write": "$uid === auth.uid"
      }
    }
  }
}